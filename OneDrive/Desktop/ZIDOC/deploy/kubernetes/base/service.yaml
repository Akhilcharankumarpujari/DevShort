apiVersion: v1
kind: Service
metadata:
  name: zidoc-service
  labels:
    app: zidoc-service
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: zidoc-service
  type: ClusterIP
